services:
  - type: web
    name: inspiration-analyzer
    env: node
    plan: free
    prebuildCommand: "npm install && npm run build && pip install -r requirements.txt"
    startCommand: "python app.py"
    buildFilter:
      paths:
        - "src/**"
        - "backend/**"
        - "package.json"
        - "requirements.txt"
